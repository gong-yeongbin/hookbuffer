# API 서버가 바인딩할 포트. 미설정 시 3001.
PORT=3001

# pino 로그 레벨 (trace|debug|info|warn|error|fatal|silent). 미설정 시 info.
LOG_LEVEL=info

# production이면 로그를 JSON 그대로, 그 외에는 pino-pretty로 사람이 읽는 형태로 낸다.
NODE_ENV=development
